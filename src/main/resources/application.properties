# This software is in the public domain under CC0 1.0 Universal plus a
# Grant of Patent License.
#
# To the extent possible under law, the author(s) have dedicated all
# copyright and related and neighboring rights to this software to the
# public domain worldwide. This software is distributed without any
# warranty.
#
# You should have received a copy of the CC0 Public Domain Dedication
# along with this software (see the LICENSE.md file). If not, see
# http://creativecommons.org/publicdomain/zero/1.0/

# =============================================================================
# Moqui Device Gateway — Quarkus standard configuration for the standalone gateway.
# =============================================================================

# -----------------------------------------------------------------------------
# HTTP server
# -----------------------------------------------------------------------------
quarkus.http.port=8081
quarkus.http.host=0.0.0.0

# -----------------------------------------------------------------------------
# REST API security
# -----------------------------------------------------------------------------
gateway.api.auth.enabled=true
gateway.api.auth.header=X-API-Key
gateway.api.auth.token=change-me-in-production

# -----------------------------------------------------------------------------
# DataSource — default (production Moqui DB: DEVICE_REQUEST, PARAMETER, DEVICE_CONFIG, …)
# -----------------------------------------------------------------------------
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/moqui
quarkus.datasource.username=moqui
quarkus.datasource.password=moqui
quarkus.datasource.jdbc.min-size=2
quarkus.datasource.jdbc.max-size=10
quarkus.datasource.jdbc.acquisition-timeout=2S
camel.sql.datasource=moquiDataSource

# -----------------------------------------------------------------------------
# DataSource "log" — telemetry DB (PARAMETER_LOG, DEVICE_LOG)
# During standalone testing both datasources can point to the same DB.
# In production point this to a separate time-series/telemetry database.
# Env var overrides: QUARKUS_DATASOURCE_LOG_JDBC_URL, QUARKUS_DATASOURCE_LOG_USERNAME, …
# -----------------------------------------------------------------------------
quarkus.datasource.log.db-kind=postgresql
quarkus.datasource.log.jdbc.url=jdbc:postgresql://localhost:5432/moqui
quarkus.datasource.log.username=moqui
quarkus.datasource.log.password=moqui
quarkus.datasource.log.jdbc.min-size=1
quarkus.datasource.log.jdbc.max-size=8
quarkus.datasource.log.jdbc.acquisition-timeout=2S
camel.sql.log.datasource=moquiLogDataSource

# -----------------------------------------------------------------------------
# Gateway logical identity
# The gateway process is represented by a Device with a matching PhysicalDevice
# row in moqui-device and is assigned to DeviceGroup rows through DeviceGroupMember with
# purposeEnumId = DgmpEdgeGateway.
# Dynamic subscription routes are restored from DB for the configured gateway.
# -----------------------------------------------------------------------------
gateway.device.id=

# -----------------------------------------------------------------------------
# Gateway request metadata SQL
# -----------------------------------------------------------------------------
gateway.request.sql=\
    SELECT dr.REQUEST_NAME, dr.DEVICE_ID, dr.PARENT_REQUEST_NAME, dr.REQUEST_TYPE_ENUM_ID, \
        dr.PURPOSE_ENUM_ID, dr.ROUTER_ENUM_ID, dr.CONNECTION_NAME, dr.BROKER_URI, \
        dr.TIMEOUT, dr.POLLING_INTERVAL, dr.QUERY, dr.ONLY_CHANGED_PARAMETERS, \
        dc.DRIVER_ENUM_ID, driver.ENUM_CODE AS DRIVER_ENUM_CODE, \
        dc.TRANSPORT_ENUM_ID, transport.ENUM_CODE AS TRANSPORT_ENUM_CODE, \
        dc.TRANSPORT_CONFIG, dc.OPTIONS \
    FROM DEVICE_REQUEST dr \
    LEFT JOIN DEVICE_CONNECTION dc ON dc.CONNECTION_NAME = dr.CONNECTION_NAME \
    LEFT JOIN ENUMERATION driver ON driver.ENUM_ID = dc.DRIVER_ENUM_ID \
    LEFT JOIN ENUMERATION transport ON transport.ENUM_ID = dc.TRANSPORT_ENUM_ID \
    WHERE dr.REQUEST_NAME = ?
gateway.request.items.sql=\
    SELECT dri.SEQUENCE_NUM, dri.PARAMETER_ID, \
        p.NUMERIC_VALUE, p.SYMBOLIC_VALUE, p.PARAMETER_ENUM_ID, \
        COALESCE(dri.QUERY, dr.QUERY) AS TARGET_QUERY \
    FROM DEVICE_REQUEST dr \
    INNER JOIN DEVICE_REQUEST_ITEM dri ON dr.REQUEST_NAME = dri.REQUEST_NAME \
    INNER JOIN PARAMETER p ON p.PARAMETER_ID = dri.PARAMETER_ID \
    LEFT JOIN ENTITY_AUDIT_LOG eal \
        ON eal.PK_PRIMARY_VALUE = dri.PARAMETER_ID \
        AND eal.CHANGED_ENTITY_NAME = 'moqui.math.Parameter' \
        AND eal.CHANGED_DATE > dr.LAST_UPDATED_STAMP \
    WHERE dr.REQUEST_NAME = ? \
        AND (dr.REQUEST_TYPE_ENUM_ID <> 'DrtWrite' \
            OR dr.ONLY_CHANGED_PARAMETERS <> 'Y' \
            OR eal.PK_PRIMARY_VALUE IS NOT NULL) \
    ORDER BY dri.SEQUENCE_NUM, dri.PARAMETER_ID

# -----------------------------------------------------------------------------
# MQTT write DeviceRequest route
# -----------------------------------------------------------------------------
mqtt.write.sql.query=\
    SELECT DISTINCT \
        dri.SEQUENCE_NUM as "sequenceNum", dri.PARAMETER_ID as "parameterId", \
        p.NUMERIC_VALUE as "numericValue", p.SYMBOLIC_VALUE as "symbolicValue", \
        p.PARAMETER_ENUM_ID as "parameterEnumId", dr.BROKER_URI as "brokerUri", \
        COALESCE(dri.QUERY, dr.QUERY) as "targetQuery" \
    FROM DEVICE_REQUEST dr \
    INNER JOIN DEVICE_REQUEST_ITEM dri ON dr.REQUEST_NAME = dri.REQUEST_NAME \
    INNER JOIN PARAMETER p ON p.PARAMETER_ID = dri.PARAMETER_ID \
    LEFT JOIN ENTITY_AUDIT_LOG eal \
        ON eal.PK_PRIMARY_VALUE = dri.PARAMETER_ID \
        AND eal.CHANGED_ENTITY_NAME = 'moqui.math.Parameter' \
        AND eal.CHANGED_DATE > dr.LAST_UPDATED_STAMP \
    WHERE dr.REQUEST_NAME = :#requestName \
        AND (dr.REQUEST_TYPE_ENUM_ID <> 'DrtWrite' \
            OR dr.ONLY_CHANGED_PARAMETERS <> 'Y' \
            OR eal.PK_PRIMARY_VALUE IS NOT NULL) \
    ORDER BY "sequenceNum"
mqtt.write.fetch.uri=sql:{{mqtt.write.sql.query}}?dataSource=#{{camel.sql.datasource}}
mqtt.write.afterPublish.enabled=true
mqtt.write.afterPublish.uri=seda:mqtt-write-device-request-audit?size=1000&blockWhenFull=false&discardWhenFull=true&waitForTaskToComplete=Never

# -----------------------------------------------------------------------------
# MQTT read DeviceRequest route
# -----------------------------------------------------------------------------
mqtt.read.route.autoStartup=true
mqtt.read.consume.uri=seda:mqtt-read-device-request-in
mqtt.read.store.log.uri=\
    sql:INSERT INTO PARAMETER_LOG \
        (PARAMETER_LOG_ID, PARAMETER_ID, SEQUENCE_NUM, OBSERVED_DATE, \
         NUMERIC_VALUE, SYMBOLIC_VALUE, PARAMETER_ENUM_ID) \
    VALUES \
        (:#parameterLogId, :#parameterId, :#sequenceNum, \
         COALESCE(:#observedDate, CURRENT_TIMESTAMP), \
         :#numericValue, :#symbolicValue, :#parameterEnumId)\
    ?batch=true&dataSource=#{{camel.sql.log.datasource}}
mqtt.read.store.parameter.uri=\
    sql:INSERT INTO PARAMETER \
        (PARAMETER_ID, NUMERIC_VALUE, SYMBOLIC_VALUE, PARAMETER_ENUM_ID, LAST_UPDATED_STAMP) \
    VALUES \
        (:#parameterId, :#numericValue, :#symbolicValue, :#parameterEnumId, CURRENT_TIMESTAMP) \
    ON CONFLICT (PARAMETER_ID) DO UPDATE SET \
        NUMERIC_VALUE = COALESCE(EXCLUDED.NUMERIC_VALUE, PARAMETER.NUMERIC_VALUE), \
        SYMBOLIC_VALUE = COALESCE(EXCLUDED.SYMBOLIC_VALUE, PARAMETER.SYMBOLIC_VALUE), \
        PARAMETER_ENUM_ID = COALESCE(EXCLUDED.PARAMETER_ENUM_ID, PARAMETER.PARAMETER_ENUM_ID), \
        LAST_UPDATED_STAMP = CURRENT_TIMESTAMP\
    ?batch=true&dataSource=#{{camel.sql.datasource}}
mqtt.read.afterStore.enabled=true
mqtt.read.afterStore.uri=seda:mqtt-read-device-request-notify?size=1000&blockWhenFull=false&discardWhenFull=true&waitForTaskToComplete=Never

# -----------------------------------------------------------------------------
# OPC UA write DeviceRequest route
# Note: the route implementation is now bean-driven and uses Camel Milo endpoint
# URIs built from DeviceConnection + DeviceRequestItem.query at runtime.
# -----------------------------------------------------------------------------
opcua.write.afterPublish.enabled=true
opcua.write.afterPublish.uri=seda:opcua-write-device-request-audit?size=1000&blockWhenFull=false&discardWhenFull=true&waitForTaskToComplete=Never

# -----------------------------------------------------------------------------
# OPC UA read DeviceRequest route
# -----------------------------------------------------------------------------
opcua.read.route.autoStartup=false
opcua.read.consume.uri=seda:opcua-read-device-request-in
opcua.read.store.log.uri=\
    sql:INSERT INTO PARAMETER_LOG \
        (PARAMETER_LOG_ID, PARAMETER_ID, SEQUENCE_NUM, OBSERVED_DATE, \
         NUMERIC_VALUE, SYMBOLIC_VALUE, PARAMETER_ENUM_ID) \
    VALUES \
        (:#parameterLogId, :#parameterId, :#sequenceNum, \
         COALESCE(:#observedDate, CURRENT_TIMESTAMP), \
         :#numericValue, :#symbolicValue, :#parameterEnumId)\
    ?batch=true&dataSource=#{{camel.sql.log.datasource}}
opcua.read.store.parameter.uri=\
    sql:INSERT INTO PARAMETER \
        (PARAMETER_ID, NUMERIC_VALUE, SYMBOLIC_VALUE, PARAMETER_ENUM_ID, LAST_UPDATED_STAMP) \
    VALUES \
        (:#parameterId, :#numericValue, :#symbolicValue, :#parameterEnumId, CURRENT_TIMESTAMP) \
    ON CONFLICT (PARAMETER_ID) DO UPDATE SET \
        NUMERIC_VALUE = COALESCE(EXCLUDED.NUMERIC_VALUE, PARAMETER.NUMERIC_VALUE), \
        SYMBOLIC_VALUE = COALESCE(EXCLUDED.SYMBOLIC_VALUE, PARAMETER.SYMBOLIC_VALUE), \
        PARAMETER_ENUM_ID = COALESCE(EXCLUDED.PARAMETER_ENUM_ID, PARAMETER.PARAMETER_ENUM_ID), \
        LAST_UPDATED_STAMP = CURRENT_TIMESTAMP\
    ?batch=true&dataSource=#{{camel.sql.datasource}}
opcua.read.afterStore.enabled=true
opcua.read.afterStore.uri=seda:opcua-read-device-request-notify?size=1000&blockWhenFull=false&discardWhenFull=true&waitForTaskToComplete=Never

# -----------------------------------------------------------------------------
# Export DeviceConfig route
# -----------------------------------------------------------------------------
device.config.export.sql.query=\
    WITH base AS ( \
        SELECT drs.RULE_SET_NAME, dr.PRIORITY, dc.DEVICE_CONFIG_ID, \
               dc.APPROXIMATED_FUNCTION_ID, \
               COALESCE(phdev.DEVICE_NAME, dr.DEVICE_ID) AS DEVICE_NAME \
        FROM DEVICE_RULE_SET drs \
        INNER JOIN DEVICE_RULE dr ON drs.DEVICE_RULE_SET_ID = dr.DEVICE_RULE_SET_ID \
        INNER JOIN DEVICE_CONFIG dc ON dr.DEVICE_CONFIG_ID = dc.DEVICE_CONFIG_ID \
        LEFT JOIN PHYSICAL_DEVICE phdev ON phdev.DEVICE_ID = dr.DEVICE_ID \
        WHERE drs.DEVICE_RULE_SET_ID = :#deviceRuleSetId \
            AND (CAST(:#deviceId AS VARCHAR) IS NULL OR dr.DEVICE_ID = :#deviceId) \
            AND (CAST(:#deviceRuleId AS VARCHAR) IS NULL OR dr.DEVICE_RULE_ID = :#deviceRuleId) \
    ), pt_base AS ( \
        SELECT b.RULE_SET_NAME, b.PRIORITY, \
               tp.APPROXIMATED_FUNCTION_ID, tp.APPROXIMATED_FUNCTION_SAMPLE_ID, \
               afs.SEQUENCE_NUM, afs.SAMPLE_VECTOR_ID, \
               tp.IS_BREAK_POINT, tp.BLENDING_ENUM_ID, tp.POINT_TIME_OFFSET_MILLIS \
        FROM base b \
        INNER JOIN TRAJECTORY_POINT tp \
            ON tp.APPROXIMATED_FUNCTION_ID = b.APPROXIMATED_FUNCTION_ID \
        INNER JOIN APPROXIMATED_FUNCTION_SAMPLE afs \
            ON afs.APPROXIMATED_FUNCTION_ID = tp.APPROXIMATED_FUNCTION_ID \
            AND afs.APPROXIMATED_FUNCTION_SAMPLE_ID = tp.APPROXIMATED_FUNCTION_SAMPLE_ID \
        WHERE b.APPROXIMATED_FUNCTION_ID IS NOT NULL \
    ) \
    SELECT \
        b.RULE_SET_NAME AS "ruleSetName", \
        b.PRIORITY AS "priority", \
        COALESCE( \
            NULLIF(MAX(dri.REQUEST_ITEM_NAME), ''), \
            NULLIF(p.PARAMETER_ALIAS, ''), \
            b.DEVICE_NAME || '.' || pd.PARAMETER_NAME \
        ) AS "parameterName", \
        CASE \
            WHEN p.NUMERIC_VALUE     IS NOT NULL THEN CAST(p.NUMERIC_VALUE AS VARCHAR(50)) \
            WHEN p.PARAMETER_ENUM_ID IS NOT NULL THEN p.PARAMETER_ENUM_ID \
            ELSE p.SYMBOLIC_VALUE \
        END AS "parameterValue" \
    FROM base b \
    INNER JOIN PARAMETER p ON b.DEVICE_CONFIG_ID = p.DEVICE_CONFIG_ID \
    INNER JOIN PARAMETER_DEF pd ON p.PARAMETER_DEF_ID = pd.PARAMETER_DEF_ID \
    LEFT JOIN DEVICE_REQUEST_ITEM dri ON dri.PARAMETER_ID = p.PARAMETER_ID \
    GROUP BY b.RULE_SET_NAME, b.PRIORITY, p.PARAMETER_ID, p.PARAMETER_ALIAS, \
             p.NUMERIC_VALUE, p.PARAMETER_ENUM_ID, p.SYMBOLIC_VALUE, \
             b.DEVICE_NAME, pd.PARAMETER_NAME, pd.PARAMETER_CODE \
    UNION ALL \
    SELECT RULE_SET_NAME, PRIORITY, \
        'Trajectory.trajectoryId', CAST(APPROXIMATED_FUNCTION_ID AS VARCHAR) \
    FROM base WHERE APPROXIMATED_FUNCTION_ID IS NOT NULL \
    UNION ALL \
    SELECT RULE_SET_NAME, PRIORITY, \
        'Trajectory.pointCount', CAST(COUNT(*) AS VARCHAR) \
    FROM pt_base GROUP BY RULE_SET_NAME, PRIORITY \
    UNION ALL \
    SELECT pb.RULE_SET_NAME, pb.PRIORITY, \
        'Trajectory.points[' || (pb.SEQUENCE_NUM - 1) || '].pos.v[' || vc.DIMENSION_INDEX || ']', \
        CAST(vc.REAL_VALUE AS VARCHAR) \
    FROM pt_base pb \
    INNER JOIN VECTOR v ON v.VECTOR_ID = pb.SAMPLE_VECTOR_ID \
    INNER JOIN VECTOR_COMPONENT vc ON vc.VECTOR_ID = v.VECTOR_ID \
    UNION ALL \
    SELECT RULE_SET_NAME, PRIORITY, \
        'Trajectory.points[' || (SEQUENCE_NUM - 1) || '].sequenceNum', \
        CAST(SEQUENCE_NUM AS VARCHAR) \
    FROM pt_base \
    UNION ALL \
    SELECT RULE_SET_NAME, PRIORITY, \
        'Trajectory.points[' || (SEQUENCE_NUM - 1) || '].timeOffsetMs', \
        CAST(COALESCE(POINT_TIME_OFFSET_MILLIS, 0.0) AS VARCHAR) \
    FROM pt_base \
    UNION ALL \
    SELECT RULE_SET_NAME, PRIORITY, \
        'Trajectory.points[' || (SEQUENCE_NUM - 1) || '].isBreakPoint', \
        CASE WHEN IS_BREAK_POINT = 'Y' THEN 'TRUE' ELSE 'FALSE' END \
    FROM pt_base \
    UNION ALL \
    SELECT RULE_SET_NAME, PRIORITY, \
        'Trajectory.points[' || (SEQUENCE_NUM - 1) || '].hasBlendingOverride', \
        CASE WHEN BLENDING_ENUM_ID IS NOT NULL THEN 'TRUE' ELSE 'FALSE' END \
    FROM pt_base \
    UNION ALL \
    SELECT RULE_SET_NAME, PRIORITY, \
        'Trajectory.points[' || (SEQUENCE_NUM - 1) || '].bufferMode', \
        CASE BLENDING_ENUM_ID \
            WHEN 'PpcmPlcAborting'     THEN '0' \
            WHEN 'PpcmPlcBuffered'     THEN '1' \
            WHEN 'PpcmPlcBlendingLow'  THEN '2' \
            WHEN 'PpcmPlcBlendingPrev' THEN '3' \
            WHEN 'PpcmPlcBlendingNext' THEN '4' \
            WHEN 'PpcmPlcBlendingHigh' THEN '5' \
            ELSE '3' \
        END \
    FROM pt_base \
    UNION ALL \
    SELECT RULE_SET_NAME, PRIORITY, \
        'Trajectory.points[' || (SEQUENCE_NUM - 1) || '].transitionMode', \
        CASE BLENDING_ENUM_ID \
            WHEN 'PpcmPlcTMNone'       THEN '0' \
            WHEN 'PpcmPlcTMCornerDist' THEN '1' \
            WHEN 'PpcmPlcTMConstVel'   THEN '2' \
            WHEN 'PpcmPlcTMStartVel'   THEN '3' \
            WHEN 'PpcmPlcTMMaxVel'     THEN '4' \
            ELSE '0' \
        END \
    FROM pt_base \
    UNION ALL \
    SELECT pb.RULE_SET_NAME, pb.PRIORITY, \
        'Trajectory.points[' || (pb.SEQUENCE_NUM - 1) || '].blendingMode', \
        CAST(COALESCE(ppp.TOLERANCE, 0.0) AS VARCHAR) \
    FROM pt_base pb \
    LEFT JOIN PARAMETRIC_PATH_POINT ppp \
        ON ppp.APPROXIMATED_FUNCTION_ID = pb.APPROXIMATED_FUNCTION_ID \
        AND ppp.APPROXIMATED_FUNCTION_SAMPLE_ID = pb.APPROXIMATED_FUNCTION_SAMPLE_ID \
    ORDER BY "priority", "parameterName"
device.config.export.fetch.uri=sql:{{device.config.export.sql.query}}?dataSource=#{{camel.sql.datasource}}
file.transfer.uri=seda:export-device-config-out?waitForTaskToComplete=Never
file.transfer.uri.2.enabled=false
file.transfer.uri.2=seda:export-device-config-out-2?size=1000&blockWhenFull=false&discardWhenFull=true&waitForTaskToComplete=Never

# -----------------------------------------------------------------------------
# Integration profile - live PostgreSQL + Artemis MQTT
# -----------------------------------------------------------------------------
%integration.gateway.api.auth.enabled=false
mqtt.broker.url=${MQTT_BROKER_URL:tcp://localhost:1883}
%integration.quarkus.datasource.db-kind=postgresql
%integration.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/moqui
%integration.quarkus.datasource.username=moqui
%integration.quarkus.datasource.password=moqui
%integration.quarkus.datasource.jdbc.min-size=1
%integration.quarkus.datasource.jdbc.max-size=5
%integration.quarkus.datasource.jdbc.acquisition-timeout=2S
# Log DB — during integration testing points to the same local DB.
# In Docker override via QUARKUS_DATASOURCE_LOG_JDBC_URL env var.
%integration.quarkus.datasource.log.db-kind=postgresql
%integration.quarkus.datasource.log.jdbc.url=jdbc:postgresql://localhost:5432/moqui
%integration.quarkus.datasource.log.username=moqui
%integration.quarkus.datasource.log.password=moqui
%integration.quarkus.datasource.log.jdbc.min-size=1
%integration.quarkus.datasource.log.jdbc.max-size=5
mqtt.read.topic=iot/parameters/in
mqtt.write.topic=iot/parameters/out
%integration.mqtt.read.consume.uri=paho-mqtt5:{{mqtt.read.topic}}?brokerUrl={{mqtt.broker.url}}&clientId=camel-gateway-test-in&userName=artemis&password=artemis
%integration.mqtt.read.afterStore.enabled=false
%integration.mqtt.read.afterStore.uri=seda:mqtt-read-device-request-notify?size=1000&blockWhenFull=false&discardWhenFull=true&waitForTaskToComplete=Never
%integration.mqtt.write.publish.uri=paho-mqtt5:{{mqtt.write.topic}}?brokerUrl={{mqtt.broker.url}}&clientId=camel-gateway-test-out&userName=artemis&password=artemis
%integration.mqtt.write.afterPublish.enabled=false
%integration.mqtt.write.afterPublish.uri=seda:mqtt-write-device-request-audit?size=1000&blockWhenFull=false&discardWhenFull=true&waitForTaskToComplete=Never
%integration.opcua.read.route.autoStartup=false
%integration.plc.log.route.autoStartup=true
%integration.plc.log.consume.uri=paho-mqtt5:moqui-plc?brokerUrl={{mqtt.broker.url}}&clientId=camel-plc-log-in&userName=artemis&password=artemis
%integration.file.transfer.uri=file:target/test-recipes
%integration.quarkus.log.category."org.apache.camel".level=DEBUG
%integration.quarkus.log.category."org.moqui.device".level=DEBUG

# -----------------------------------------------------------------------------
# Local dev profile: PostgreSQL localhost + configurable MQTT broker
# Usage: java -Dquarkus.profile=local -jar target/quarkus-app/quarkus-run.jar
# -----------------------------------------------------------------------------
%local.gateway.api.auth.enabled=false
%local.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/moqui
%local.quarkus.datasource.username=moqui
%local.quarkus.datasource.password=moqui
%local.quarkus.datasource.jdbc.min-size=1
%local.quarkus.datasource.jdbc.max-size=5
%local.quarkus.datasource.log.db-kind=postgresql
%local.quarkus.datasource.log.jdbc.url=jdbc:postgresql://localhost:5432/moqui
%local.quarkus.datasource.log.username=moqui
%local.quarkus.datasource.log.password=moqui
%local.quarkus.datasource.log.jdbc.min-size=1
%local.quarkus.datasource.log.jdbc.max-size=5
%local.mqtt.read.route.autoStartup=true
%local.mqtt.read.consume.uri=paho-mqtt5:iot/parameters/in?brokerUrl={{mqtt.broker.url}}&clientId=camel-gateway-in&userName=artemis&password=artemis
%local.mqtt.read.afterStore.enabled=false
%local.mqtt.read.afterStore.uri=seda:mqtt-read-device-request-notify?size=1000&blockWhenFull=false&discardWhenFull=true&waitForTaskToComplete=Never
%local.mqtt.write.publish.uri=paho-mqtt5:iot/parameters/out?brokerUrl={{mqtt.broker.url}}&clientId=camel-gateway-out&userName=artemis&password=artemis
%local.mqtt.write.afterPublish.enabled=true
%local.mqtt.write.afterPublish.uri=http://localhost:8080/rest/s1/deviceGateway/requests/callback?bridgeEndpoint=true&throwExceptionOnFailure=false&httpMethod=POST
%local.plc.log.route.autoStartup=true
%local.plc.log.consume.uri=paho-mqtt5:moqui-plc?brokerUrl={{mqtt.broker.url}}&clientId=camel-plc-log-in&userName=artemis&password=artemis
%local.opcua.read.route.autoStartup=false
%local.opcua.read.consume.uri=seda:opcua-read-device-request-in
%local.opcua.write.afterPublish.enabled=true
%local.opcua.write.afterPublish.uri=http://localhost:8080/rest/s1/deviceGateway/requests/callback?bridgeEndpoint=true&throwExceptionOnFailure=false&httpMethod=POST
%local.file.transfer.uri=file:target/test-recipes
%local.quarkus.log.category."org.apache.camel".level=DEBUG
%local.quarkus.log.category."org.moqui.device.gateway".level=DEBUG

# -----------------------------------------------------------------------------
# Production override examples
# -----------------------------------------------------------------------------
# mqtt.broker.url=tcp://localhost:1883
# mqtt.clientId=moqui-gateway-1
# mqtt.write.publish.uri=paho-mqtt5:${mqtt.write.topic}?brokerUrl=${mqtt.broker.url}&clientId=${mqtt.clientId}_out
# mqtt.write.afterPublish.uri=sql:UPDATE DEVICE_REQUEST SET LAST_UPDATED_STAMP = CURRENT_TIMESTAMP WHERE REQUEST_NAME = :#requestName?dataSource=#{{camel.sql.datasource}}
# mqtt.read.consume.uri=paho-mqtt5:${mqtt.read.topic}?brokerUrl=${mqtt.broker.url}&clientId=${mqtt.clientId}_in
# mqtt.read.afterStore.uri=http://moqui-server:8080/rest/s1/org/moqui/device/DeviceGatewayServices/write/MqttReadDeviceRequest
# file.transfer.uri=sftp:${sftp.user}@${sftp.host}:${sftp.port}${sftp.remote.dir}
# file.transfer.uri.2.enabled=false
# file.transfer.uri.2=sftp:${sftp.user}@plc2.example.com:${sftp.port}${sftp.remote.dir}
# sftp.host=plc.example.com
# sftp.port=22
# sftp.user=admin
# sftp.remote.dir=/recipe

# -----------------------------------------------------------------------------
# PLC diagnostic log route (CODESYS LoggerFacade batch format → PARAMETER_LOG)
# The plc-log-ingest route transforms the IEC 61131 numbered-object format,
# ensures PARAMETER_DEF + PARAMETER rows, then delegates to store-device-request-inbound.
# Override plc.log.consume.uri via the integration/local profile or production overrides.
# -----------------------------------------------------------------------------
plc.log.route.autoStartup=false
plc.log.consume.uri=seda:plc-log-in
plc.log.store.ensure.parameter.def.uri=\
    sql:INSERT INTO PARAMETER_DEF \
        (PARAMETER_DEF_ID, PARAMETER_TYPE_ENUM_ID, PARAMETER_CODE, PARAMETER_NAME) \
    VALUES \
        ('PlcLoggerDef', 'PtTextShort', 'PLC-LOG', 'PLC Logger') \
    ON CONFLICT (PARAMETER_DEF_ID) DO NOTHING\
    ?dataSource=#{{camel.sql.log.datasource}}
plc.log.store.ensure.parameter.uri=\
    sql:INSERT INTO PARAMETER (PARAMETER_ID, PARAMETER_DEF_ID) \
    VALUES (:#parameterId, 'PlcLoggerDef') \
    ON CONFLICT (PARAMETER_ID) DO NOTHING\
    ?batch=true&dataSource=#{{camel.sql.log.datasource}}
plc.log.store.device.log.uri=\
    sql:INSERT INTO DEVICE_LOG \
        (DEVICE_LOG_ID, DEVICE_ID, PAYLOAD, SEQUENCE_NUM, OBSERVED_DATE) \
    VALUES \
        (:#deviceLogId, :#deviceId, :#payload, :#sequenceNum, \
         COALESCE(:#observedDate, CURRENT_TIMESTAMP))\
    ?batch=true&dataSource=#{{camel.sql.log.datasource}}

# -----------------------------------------------------------------------------
# Inbound error notification
# Sends a REST callback to Moqui when a read route fails to persist data to
# PARAMETER or PARAMETER_LOG continuously for longer than the threshold.
# Reads always continue — they are never blocked by storage failures.
# Set notification.uri to a Camel endpoint (e.g. http://moqui-server:8080/rest/...).
# Leave blank to log-only without sending a remote notification.
# -----------------------------------------------------------------------------
gateway.inbound.error.notification.enabled=true
gateway.inbound.error.notification.threshold.seconds=60
gateway.inbound.error.notification.uri=

# -----------------------------------------------------------------------------
# Logging / health
# -----------------------------------------------------------------------------
quarkus.log.level=INFO
quarkus.log.category."org.apache.camel".level=INFO
quarkus.log.category."org.moqui.device.gateway".level=INFO
quarkus.log.category."org.apache.camel.component.milo".level=WARN
quarkus.log.category."org.apache.camel.component.paho".level=WARN
quarkus.log.category."io.agroal".level=WARN
quarkus.log.console.async.enabled=true
quarkus.log.console.async.queue-length=4096
quarkus.log.console.async.overflow=discard
quarkus.log.file.enabled=true
quarkus.log.file.path=logs/moqui-device-gateway.log
quarkus.log.file.async.enabled=true
quarkus.log.file.async.queue-length=8192
quarkus.log.file.async.overflow=discard
quarkus.log.file.rotation.max-file-size=25M
quarkus.log.file.rotation.max-backup-index=8
quarkus.log.file.rotation.rotate-on-boot=true
quarkus.smallrye-health.root-path=/q/health
